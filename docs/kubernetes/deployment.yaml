apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-api
spec:
  selector:
    matchLabels:
      app: tenant-api
  replicas: 1
  template:
    metadata:
      labels:
        app: tenant-api
    spec:
      serviceAccount: tenant-api
      containers:
      - name: tenant-api
        image: ghcr.io/natron-io/tenant-api:latest
        ports:
        - containerPort: 8000
